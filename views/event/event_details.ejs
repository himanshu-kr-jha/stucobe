<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= event.title %> - Details</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-12">
        <div class="max-w-3xl mx-auto bg-white rounded-lg shadow-md overflow-hidden">
            <!-- Event Image (Placeholder) -->
            <div class="h-64 bg-gray-200 flex items-center justify-center">
                <span class="text-gray-500">Event Banner</span>
            </div>

            <!-- Event Details -->
            <div class="p-8">
                <span class="inline-block bg-<%= event.category.toLowerCase() === 'technical' ? 'blue' : 'green' %>-100 text-<%= event.category.toLowerCase() === 'technical' ? 'blue' : 'green' %>-800 text-xs font-semibold px-2 py-1 rounded mb-4">
                    <%= event.category %>
                </span>
                <h1 class="text-3xl font-bold mb-4"><%= event.title %></h1>
                <p class="text-gray-700 mb-6"><%= event.description %></p>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
                    <div class="bg-gray-50 p-4 rounded-lg">
                        <h3 class="font-semibold mb-2">📅 Date & Time</h3>
                        <p><%= event.date.toLocaleDateString() %></p>
                        <p><%= event.time %></p>
                    </div>
                    <div class="bg-gray-50 p-4 rounded-lg">
                        <h3 class="font-semibold mb-2">📍 Location</h3>
                        <p><%= event.location %></p>
                    </div>
                </div>

                <!-- Action Buttons -->
                <div class="flex space-x-4">
                    <a href="/events" class="bg-gray-300 hover:bg-gray-400 text-gray-800 px-4 py-2 rounded-md">
                        ← Back to Events
                    </a>
                    <a href="#" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-md">
                        Register for Event
                    </a>
                </div>
            </div>
        </div>
    </div>
</body>
</html> 