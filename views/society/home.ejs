<style>    
    /* Hero Section */
/* Hero Section */
.hero {
    background: #f8f9fa; /* Light grey background for a classic feel */
    color: #212529; /* Dark text for contrast */
    text-align: center;
    padding: 3rem 1rem;
    border-bottom: 1px solid #e9ecef; /* Subtle border for section distinction */
}

.hero h1 {
    font-size: 2.5rem;
    font-weight: 600;
    margin-bottom: 1rem;
}

.hero a {
    background-color: #007bff; /* Primary blue */
    color: #fff;
    padding: 0.5rem 1.5rem;
    text-decoration: none;
    font-size: 1rem;
    border-radius: 0.25rem;
    transition: background-color 0.3s;
}

.hero a:hover {
    background-color: #0056b3; /* Darker blue */
}

/* Card Section */
.container-home {
    display: flex;
    justify-content: center;
    gap: 1.5rem;
    margin: 2rem auto;
    flex-wrap: wrap;
    padding: 0 1rem;
}

.card-list {
    background-color: #fff;
    border: 1px solid #dee2e6; /* Subtle border */
    border-radius: 1rem; /* Rounded corners for a modern look */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    width: 20rem;
    position: relative;
    height: 450px;
}

.card-list:hover {
    transform: translateY(-10px);
    box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
}

.card-img-top-list {
    width: 100%;
    height: 270px;
    object-fit: cover;
    border-bottom: 1px solid #dee2e6; /* Separating the image */
}

.card-body-list {
    padding: 0.1rem;
}

.card-text-list {
    font-size: 1rem;
    color: #495057; /* Subtle grey text */
    margin-bottom: 0.5rem;
    line-height: 1.5;
}

.text-muted {
    color: #6c757d; /* Classic muted text color */
    font-size: 0.9rem;
}

button {
    background-color: #28a745; /* Success green */
    color: #fff;
    border: none;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    margin:1rem;
    cursor: pointer;
    font-size: 1rem;
    width: 30% !important;
    transition: background-color 0.3s, transform 0.2s;
}

button:hover {
    background-color: #218838; /* Darker green */
    transform: scale(1.05);
}

form {
    margin-top: 1rem;
}

/* Link Styling */
a {
    text-decoration: none;
    color: inherit;
    transition: color 0.3s;
}

a:hover {
    color: #0056b3;
}

/* Additional Card Styling */
.card-list::after {
    content: '';
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    height: 5px;
    width: 100%;
    background: linear-gradient(90deg, #007bff, #28a745);
}
/* Hero Section */
.hero {
    background: #f0f4f8; /* Light grayish-blue for a fresh look */
    color: #2d2d2d; /* Dark text for good readability */
    text-align: center;
    padding: 4rem 1rem;
    border-bottom: 1px solid #e0e0e0; /* Subtle border for clean distinction */
}

.hero h1 {
    font-size: 2.8rem;
    font-weight: 700;
    margin-bottom: 1.5rem;
    color: #333;
}

.hero a {
    background-color: #007bff; /* Primary blue */
    color: #fff;
    padding: 0.8rem 2rem;
    text-decoration: none;
    font-size: 1.2rem;
    border-radius: 2rem;
    transition: background-color 0.3s, transform 0.3s;
}

.hero a:hover {
    background-color: #0056b3; /* Darker blue */
    transform: scale(1.05); /* Subtle scaling on hover */
}

/* Container and Card Section */
.container-home {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin: 3rem auto;
    flex-wrap: wrap;
    padding: 0 1rem;
    max-width: 1200px;
}

.card-list {
    background-color: #fff;
    border: 1px solid #e0e0e0; /* Subtle border */
    border-radius: 1rem;
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
    overflow: hidden;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    width: 20rem;
    height: 500px;
    position: relative;
    margin-bottom: 2rem;
}

.card-list:hover {
    transform: translateY(-15px);
    box-shadow: 0 18px 36px rgba(0, 0, 0, 0.15);
}

.card-img-top-list {
    width: 100%;
    height: 250px;
    object-fit: cover;
    border-bottom: 1px solid #e0e0e0; /* Separating the image */
    transition: transform 0.3s ease;
}

.card-img-top-list:hover {
    transform: scale(1.1); /* Zoom effect on hover */
}

.card-body-list {
    padding: 1rem;
}

.card-text-list {
    font-size: 1.1rem;
    color: #333;
    margin-bottom: 1rem;
    line-height: 1.6;
    font-weight: 500;
}

.text-muted {
    color: #868e96;
    font-size: 0.95rem;
}

button {
    background-color: #28a745; /* Success green */
    color: #fff;
    border: none;
    padding: 0.8rem 1.5rem;
    border-radius: 2rem;
    margin-top: 1rem;
    cursor: pointer;
    font-size: 1.1rem;
    transition: background-color 0.3s, transform 0.2s;
    width: 100%;
    display: inline-block;
}

button:hover {
    background-color: #218838; /* Darker green */
    transform: scale(1.05);
}

/* Form Styling */
form {
    margin-top: 1rem;
}

form input, form button {
    width: 100%;
    padding: 0.8rem 1rem;
    margin: 0.5rem 0;
    border-radius: 0.5rem;
    font-size: 1rem;
    border: 1px solid #e0e0e0;
}

/* Link Styling */
a {
    text-decoration: none;
    color: inherit;
    transition: color 0.3s;
}

a:hover {
    color: #007bff;
}

/* Additional Card Styling */
.card-list::after {
    content: '';
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    height: 5px;
    width: 100%;
    background: linear-gradient(90deg, #007bff, #28a745);
}

/* Responsive Styles */
@media (max-width: 768px) {
    .container-home {
        flex-direction: column;
        align-items: center;
        gap: 2rem;
    }

    .card-list {
        width: 90%; /* Full width on smaller screens */
    }
}

@media (max-width: 480px) {
    .hero h1 {
        font-size: 2rem;
    }

    .hero a {
        font-size: 1rem;
        padding: 0.6rem 1.5rem;
    }

    .card-list {
        width: 100%;
        height: auto;
    }

    .card-img-top-list {
        height: 200px;
    }

    .card-body-list {
        padding: 1rem;
    }
}

</style>
<% layout("layouts/boilerplate") %>
<body class="vsc-initialised">
    <!-- Hero Section -->
    <div class="hero">
        <h1>Trending societies</h1>
    </div>

    <!-- Content Section -->
    <div class="container container-home">
        <% societies.forEach(function(society) { %>
            <a href=<%=`/society/${society._id}`%> >
                <div class="card-list" style="width:20rem">
                
                    <img src="<%=society.logo%>" alt="Card View" class="card-img-top-list">
                    <div class="card-img-overlay-list">
                    </div>
                <div class="card-body-list">
                        <p class="card-text-list">
                            <b><%= society.name %></b>
                            <br>
                            <span class="text-muted"><%= society.description.slice(0,10) %>...</span>
                            <%if(society.followers!=undefined){%>
                                <p>Followers:<%= society.followers.length%></p>
                            <%}%>
                        </p>
                </div>
                <%if(currUser!=undefined && !society.followers.includes(currUser._id)){%>
                    <form action=<%=`/user/${society._id}/follow`%> method=post>
                        <button class="btn btn-outline-primary">Follow</button>
                    </form>
                <%}else{%>
                    <p>You are following.</p>
                <%}%>
                
        </div>
        </a>
        <% }); %>
    </div>
</body>
